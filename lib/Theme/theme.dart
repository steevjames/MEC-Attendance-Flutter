import 'package:flutter/material.dart';

// General

String fontName = 'default';

// Splash screen

var splashGradientStart = Color(0xff2c9fc0);
var splashGradientEnd = Color(0xff3464BB);

// Attendance page

Color gradientAppbarStart = Colors.cyan;
Color gradientAppbarEnd = Colors.indigo;

Color attendaneGradientStart = Color(0xff1c9fc0);
Color attendaneGradientEnd = Color(0xff3464BB);

Color gradientWhenUnderStart = Color(0xFF880000);
Color gradientWhenUnderEnd = Color(0xFF880000);

Color floatingButtonColor = Color(0xFF2c7ec4);
Color pageBackgroundColor = Color(0xFFe7e7e7);

// Timetable page

Color gradientTimetableCircleStart = Color(0xFF19AAD5);
Color gradientTimetableCircleEnd = Color(0xFF2680C1);
